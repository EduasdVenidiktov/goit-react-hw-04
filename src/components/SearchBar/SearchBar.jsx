<header>
  <form>
    <input
      type="text"
      autocomplete="off"
      autofocus
      placeholder="Search images and photos"
    />
    <button type="submit" onSubmit={() => onSubmit()}>
      Search
    </button>
  </form>
</header>;
